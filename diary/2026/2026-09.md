## 2026-09-01

### 学習時間 3

- Go 言語で学ぶ実践 gRPC 入門 1
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：ハーネス=馬具。走り出す前に「どう走ってほしいか」を伝える、事前設計の層。CLAUDE.md、SKills、plan、サブエージェント
- 記憶：ガードレール=道を外れないための柵。走った後に「逸脱していないか」を機械で自動検証する。事後検証の層。静的解析（lint・フォーマット・型チェック）、ビルド、テスト（ユニット・結合・E2E）
- 記憶：.proto でサービスにメソッドとして定義した`ListFiles`について、クライアント側で`client.ListFiles`を呼ぶとサーバ側で実装した`(*server).ListFiles`が呼ばれる。これが RPC の呼び出しの流れ。
- 記憶：gRPC の Interceptor は、一般的に良く使われるものがライブラリとして提供されている。interceptor を使用することで、リクエストの前、レスポンスの後に処理を挟むことができる。
- 記憶：Interceptor は middleware という位置づけでライブラリが提供されている。`grpc-ecosystem/go-grpc-middleware`がそれ。
- 記憶：Hono の streamSSE は HTTP を SSE 用に切り替えるもの。stream は streamSSE がコールバックの引数に渡してくるオブジェクト。stream.writeSSE で SSE 接続の中で 1 つのイベントを書かく。streamSSE の中で複数回書くことが可能。
- 記憶：streamSSE は海鮮を開くだけ。stream.writeSSE は実際に送信を粉う
- 記憶：422 Unprocessable Content（リクエストは構文的に正しい。参照しているモデルを処理できない）

## 2026-09-02

### 学習時間 3

- Go 言語で学ぶ実践 gRPC 入門 1
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：gRPC では独自のステータスコードが定義されており、gRPC のエラーコードを使用してエラーハンドリングを行うことができる
- 記憶：REST API は 400 番台や 500 番台などの、標準化された HTTP ステータスコードを使ってエラーハンドリングを行う。gRPC はこれと併せて独自のエラーコードを持っている

## 2026-09-03

### 学習時間 3

- Go 言語で学ぶ実践 gRPC 入門 1
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：DI コンテナーは、「どのインタフェースにどの実装クラスを使うか」を一箇所で管理し、必要な時に自動的に適切なインスタンスを提供する仕組み。依存関係の注入ロジックを複数の場所で書かなくても済む。
- 記憶：gRPC の DeadLine は context.Withtimeout で実装することができる
- 記憶：「useOptimistic がないと楽観的更新ができない」ではなく、「useOptimistic を使うと楽観的状態を確定状態から分離して扱いやすくなる」と理解する
- 記憶：<Activity>は React 標準の API で、このコンポーネントで囲まれた要素は、`<Activity mode="hidden">`でもプリレンダリングされる。
- 記憶：useDefferredValue は遅延中はひとつ前の状態を保持し続ける
- 記憶：useTransition の戻り値である、startTransition 関数のなかで実行される状態更新は、優先度が低い更新とみなれる。優先度が低い state 更新は、ユーザー入力などのより優先度が高い更新により中断される
- 記憶：更新中かどうかは isPending で取得できる。テキストボックスの入力によって、結果の表示が同的に変わる場合、入力をブロックせず、入力が終わってからステートの更新がされるような動きを実現できる
- 記憶：子コンポーネントで use(resources)とし、親コンポーネントのフェッチ結果を受け取ることで、親コンポーネントのフェッチ中は子コンポーネントは Promise 状態となる（Suspense が使える）
- 記憶：useCallback の第二引数に、関数内で参照しているステートを追加しないと、関数を呼び出しても、関数内のステートの値は更新されない。関数の外ではステートの値が更新されていたとしても、関数内だけ古い状態となる
- 記憶：上記の理由は、第二引数に入れた変数の変更がない限り、関数自体が再生成されない（以前作成されたときのまま=キャッシュされたもの）ため

## 2026-09-04

### 学習時間 2.5

- Go 言語で学ぶ実践 gRPC 入門 0.5
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

-

## 2026-09-05

### 学習時間 10.5

- Go 言語で学ぶ実践 gRPC 入門 0
- Don't Pay for Claude Code | Build This Instead 0
- アクティブリコール（直近 1 ヵ月の復習）2
- 作りながら学ぶ！ドメイン駆動設計実践入門 1.5
- アプリケーション開発（社外ファイル共有）7

### 振り返り

- 記憶：Ingress は、k8s のルーティングをするための Services オブジェクト。Deployment にルーティング（配送先の ClusterIP の名前を指定）する。

## 2026-09-06

### 学習時間 9

- Go 言語で学ぶ実践 gRPC 入門 1.5
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1.5
- アプリケーション開発（社外ファイル共有）5

### 振り返り

- 記憶：trip.proto ファイルで`option go_package = "shared/proto/trip;trip";`としたときの`shared/proto/trip`は import したときのパスを示す。後ろの`trip`はパッケージ名。
- 記憶：hc は Hono 公式の RPC クライアント生成関数。サーバ側が export した AppType をジェネリクスに渡し、ベース URL を指定して、ルート構造をそのままオブジェクトにしたクライアントを作る。
- 記憶：関数呼び出しの際の void（`void resume({ mode: last.mode, model: last.model })`など）は、同期コンテキストから async 関数を呼び出すときにつける。実行だけして結果を破棄するときに使う。void をつけると Promise は破棄される

## 2026-09-07

### 学習時間 3

- Go 言語で学ぶ実践 gRPC 入門 1
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：集約（Review）をイベントから復元するときは、イベントを過去から順番に充てていくと充てた時点の集約が復元できる。
- 記憶：イベントソーシングでは集約の状態は保存しない。保存されたイベントが唯一の情報源として機能する
- 記憶：k8s の中では、Service 名が DNS 名になる。deployment ファイルの`metadata:→name: trip-service`が DNS 名になる。

## 2026-09-08

### 学習時間 3

- Go 言語で学ぶ実践 gRPC 入門 1
- Don't Pay for Claude Code | Build This Instead 0.5
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：k8s では kind が ConfigMap の中に環境変数の値を定義（app-config.yaml）とし、それを参照元の Service や Deployment（Pod）から`env.valueFrom.name:app-config`等として参照する。

## 2026-09-09

### 学習時間 1.25

- Go 言語で学ぶ実践 gRPC 入門 0.5
- Don't Pay for Claude Code | Build This Instead 0
- 作りながら学ぶ！ドメイン駆動設計実践入門 0.75

### 振り返り

- 記憶：graceful shutdown において、`sigCh := make(chan os.Signal, 1)`で作成したチャンネルは`signal.Notify(sigCh, os.Interrupt, syscall.SIGTERM)`で`SIGINT`か`SIGTERM`が来るまで待ち続ける

## 2026-09-10

### 学習時間 3

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 1
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：クリーンアーキテクチャは依存性逆転の原則（DIP）こそが本質。層の数は問題ではない。依存の向きが中心に向かっていればクリーンアーキテクチャ
- 記憶：Go の「implicit interface（暗黙的な interface 満足）」は JS のようにインタフェースを明示的に継承しなくても「必要なメソッドを持っていれば、その interface 型として静的に扱える」仕組み
- 記憶：似たような概念として、python の Duck Typing がある。オブジェクトの型をチェックせず、必要なメソッドを持っていれば、そのオブジェクトをその型として扱う。「アヒルのように歩き、アヒルのように鳴くなら、それはアヒルとして扱っていいだろう」という考え方。

## 2026-09-11

### 学習時間 3

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0.75
- Don't Pay for Claude Code | Build This Instead 1
- 作りながら学ぶ！ドメイン駆動設計実践入門 1

### 振り返り

- 記憶：エンティティは集約が、何についての集約かを識別するための情報を提供する値オブジェクト（個体識別に特化した値オブジェクト）
- 記憶：エンティティは集約を特定するための複数の要素（値オブジェクト、プリミティブ値）をもつ。識別 ID のようなもの（値オブジェクト）と併せて、人間が識別できる情報（値オブジェクト）を併せて入れる
- 記憶：何であるか → エンティティ。何ができるか → 集約。

## 2026-09-12

### 学習時間 7.25

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 1.5
- Don't Pay for Claude Code | Build This Instead 1.15
- 作りながら学ぶ！ドメイン駆動設計実践入門 1.5
- アプリケーション開発（社外ファイル共有）3

### 振り返り

- 記憶：エンティティは集約が、何についての集約かを識別するための情報を提供する値オブジェクト（個体識別に特化した値オブジェクト）
- 記憶：エンティティは集約を特定するための複数の要素（値オブジェクト、プリミティブ値）をもつ。識別 ID のようなもの（値オブジェクト）と併せて、人間が識別できる情報（値オブジェクト）を併せて入れる
- 記憶：何であるか → エンティティ。何ができるか → 集約。

## 2026-09-13

### 学習時間 1.5

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0.5
- Don't Pay for Claude Code | Build This Instead 0.5
- 作りながら学ぶ！ドメイン駆動設計実践入門 0.5

### 振り返り

- 記憶：識別子に関するエンティティと値オブジェクトの関係について、BookId では ISBN に関するドメインロジック（規則性）を定義する。BooIdIdentity では BookId を使用した同一性確認とその他ユーザが識別できる情報（bookId、title、author）を定義する。
- 記憶：Go の NamedType(名前付き型)は`type UserID string`や`type OrderID string`のように定義したときに全社で定義した変数を後者に代入すると型エラーになる（元は同じ string なのに）
- 記憶：Go の iota は、Go の定数（const）を連番で定義するための特殊な識別子。const ブロックの中では、iota は 0 から始まって、1 行ごとに自動的に 1 増える。自動で連番を採番したいときに使う

## 2026-09-14

### 学習時間 2.25

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 1
- Don't Pay for Claude Code | Build This Instead 0.5
- 作りながら学ぶ！ドメイン駆動設計実践入門 0.75

### 振り返り

- 記憶：DDD における集約（Aggregate）の不変条件は、「注文金額は 0 以上でなければならない」「在庫数はマイナスにならない」といった、ビジネス上常に成立しなければならないルールを指す

## 2026-09-15

### 学習時間 3.25

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0
- Don't Pay for Claude Code | Build This Instead 0.5
- 作りながら学ぶ！ドメイン駆動設計実践入門 0.5
- 実演！ミノ駆動さんに AI 製のコードを叩き直してもらった 2.25

### 振り返り

- 記憶：アプリケーションサービスのテストは、ユースケースの成功・失敗シナリオに焦点を当てる。ビジネスロジックについてはドメインモデルのテストで検証済を前提とする。
- 記憶：アプリケーション開発でやっていること。目的（何がやりたいか）→ 要件（達成する条件）→ 仕様（どのように達成するか）
- 記憶：集約の分け方は目的毎に分ける（関心の分離、目的の分離）。即時注文クラス、予約注文クラス、抽選注文クラス。「注文」という日本語で見ず、本質（目的）に着目する。
- 記憶：ミノ駆動さんはオニオンアーキテクチャ+CQRS を進めている。
- 記憶：CQRS では更新系は、ドメイン層でバリデーションチェックするが、参照系は、ユースケースから直接インフラストラクチャ層にアクセスする。
- 記憶：ミノ駆動さんは、外部サービス系はユースケースから直接インフラストラクチャ層を参照している（インフラストラクチャ層は外部の場合は Gateway、内部は更新系はリポジトリ、参照系はクエリサービスとしている）
- 記憶：ドメインモデル、リードモデル（DTO）、データベースモデルはそれぞれ異なる
- 記憶：ドメインモデルは集約に近いイメージ。リードモデル（記事とコメント等）は読み取りように定義されたモデル。データベースモデルは保存するためのモデル（プリミティブ・値のみ）
- 記憶：サービスより、ユースケースのようがいいかも。サービスとつけるとスーパークラスになりやすい。
- 記憶：仕様が変わったということは、要件が変わり、つまりは目的が変わったということ。
- 記憶：ドメインモデル=ドメインモデル=集約(Aggregate)
- 記憶：メソッド・関数は制御抽象化。クラス（集約）はデータ抽象化。**目的別に抽象化するといい。**

## 2026-09-16

### 学習時間 1.25

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0.5
- Don't Pay for Claude Code | Build This Instead 0.5
- セキュア API 設計・構築・実装を貫く原則 0.25

### 振り返り

## 2026-09-17

### 学習時間 2

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0.5
- Don't Pay for Claude Code | Build This Instead 1
- セキュア API 設計・構築・実装を貫く原則 0.5

### 振り返り

- セキュア・バイ・デザイン：セキュリティを後付けでなく設計段階から組み込むという考え方

## 2026-09-18

### 学習時間 3

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0.5
- Don't Pay for Claude Code | Build This Instead 1
- セキュア API 設計・構築・実装を貫く原則 0.5

### 振り返り

-

## 2026-09-19

### 学習時間 7.5

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 1.5
- Don't Pay for Claude Code | Build This Instead 1
- セキュア API 設計・構築・実装を貫く原則 0
- Git 研修【MIXI 25 新卒技術研修】1.5
- アプリケーション開発（社外ファイル共有）2
- アプリケーション開発（運転記録管理）1.5

### 振り返り

- 記憶：git commit -amend で直前の commit を修正できる（変更を直前のコミットに反映できる）
- 記憶：GitFlow の方が GitHubFlow よりも複雑である
- 記憶：GItFlow は develop ブランチを作成しその子供に feature/*をいくつも持つ構成。feature/*は develop ブランチに PR を出し、マージする。リリース前には release ブランチを作成し、最後 main に PR を出してマージする
- 記憶：GitHubFlow は main ブランチの feature/*ブランチから構成される。feature/*ブランチの作業が完成したら、PR を出して main にマージする。
- 記憶：私の場合。main と develop の 2 段構成。develop = 次回リリース候補を作る場所。develop 直か、develop の子供に feature/\*を作る。
- 記憶：squash は複数のコミット履歴を 1 つのコミットにまとめること。Git の履歴を「開発途中の記録」ではなく「意味のある変更の単位」に整理するために使うことが多い。
- 記憶：main A に対して、feature で A -- B -- C -- D -- E としたときに PR を出す前に、squash すると feature のコミット履歴が A -- F となる。F が B + C + D + E をまとめたコミット。その後 PR を出す。

## 2026-09-20

### 学習時間 5.75

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 0
- Don't Pay for Claude Code | Build This Instead 0
- セキュア API 設計・構築・実装を貫く原則 0
- Git 研修【MIXI 25 新卒技術研修】1.5
- 現役 SRE エンジニアが選ぶ！PC 作業効率爆上がり Raycast の使い方を教えます 1.5
- 今日からはじめる Claude Design。パワポを卒業して、専属デザインシステムを構築する方法。1
- アプリケーション開発（車両運行記録管理）1.75

### 振り返り

- 記憶：コメントだけ修正したいときは`git commit --amend -m "ユーザー一覧画面を追加"`でコメントだけ修正できる

## 2026-09-21

### 学習時間 8.5

- Go の設計、どこまでやる？〜小さく作って、壊さず育てる〜 1
- Don't Pay for Claude Code | Build This Instead 0.5
- セキュア API 設計・構築・実装を貫く原則 0
- Git 研修【MIXI 25 新卒技術研修】1.5
- アプリケーション開発（車両運行記録管理）2.5
- アプリケーション開発（社外ファイル共有）3

### 振り返り

- 記憶：git reflog は、HEAD の指していたコミットの履歴が見れる。間違えて reset してしまった場合も reflog で reset 前のハッシュや HEAD@{n}を確認して git reset --hard HEAD@{1}等として元のコミットに戻すことができる。
- 記憶：go.work（Go の Workspace）は複数の Module を同時開発したいときに使用する。use で囲んだモジュールを開発中だけひとまとまりとして扱うための仕組み
- 記憶：DI をする場所（ファイル）をコンポジションルートという。コンポジションルートだけは、すべてのレイヤーを import してよい。最も外側のレイヤー（go でいうと main.go など）だからこそ許される
